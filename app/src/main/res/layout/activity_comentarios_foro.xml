<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/mainLayout"
    tools:context=".activity.ComentariosForoActivity">

    <RelativeLayout
        android:id="@+id/layout_mensaje_fijado"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:layout_margin="16dp"
        android:background="@color/fondoFijado"
        app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent">

    <ImageView
        android:id="@+id/image_pin"
        android:layout_width="14dp"
        android:layout_height="14dp"
        android:src="@drawable/ic_pin"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_margin="0dp" />

    <TextView
        android:id="@+id/text_view_mensaje"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:text="Mensaje destacado aquí"
        android:padding="12dp"
        android:textColor="@color/white"
        android:textSize="14sp"
        android:textStyle="bold"
        android:linksClickable="true"
        android:autoLink="web"
        android:gravity="left"
        app:elevation="4dp"
        android:layout_below="@id/image_pin"
        android:layout_marginTop="8dp"/>

</RelativeLayout>



    <!-- RecyclerView para mostrar los comentarios -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_comentarios"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_margin="16dp"
        app:layout_constraintTop_toBottomOf="@id/layout_mensaje_fijado"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/layout_add_comment" />

    <!-- Layout para agregar un nuevo comentario -->
    <LinearLayout
        android:id="@+id/layout_add_comment"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="8dp"
        android:background="@color/colorMorado2"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:gravity="center_vertical"> <!-- Alineación vertical centrada -->

        <!-- Campo de entrada para el comentario -->
        <EditText
            android:id="@+id/edit_text_comentario"
            android:layout_width="0dp"
            android:textColorHint="@color/white2"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="Escribe un comentario..."
            android:inputType="text"
            android:maxLines="3"
            android:padding="8dp"
            android:textColor="@color/white"
            android:backgroundTint="@color/white"/>

        <!-- FrameLayout con el botón de enviar comentario -->
        <FrameLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginStart="8dp"
            android:layout_marginLeft="8dp"
        android:layout_marginTop="0dp"
        android:layout_marginBottom="0dp">

        <ImageButton
            android:id="@+id/button_enviar_comentario"
            android:layout_width="122dp"
            android:layout_height="41dp"
            android:background="@drawable/rounded_button_background" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:fontFamily="@font/pixeli"
            android:text="Enviar"
            android:textColor="@android:color/white"
            android:textSize="20sp"
            android:textStyle="bold" />
    </FrameLayout>

</LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
