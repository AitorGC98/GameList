<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/main"
    android:padding="16dp"
    android:fitsSystemWindows="true">

    <!-- Agregar un ScrollView envolviendo el contenido -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- Imagen de Perfil -->
            <com.google.android.material.imageview.ShapeableImageView
                android:id="@+id/BotonPerfil"
                android:layout_width="200dp"
                android:layout_height="200dp"
                android:layout_marginTop="60dp"
                android:clickable="true"
                android:clipToOutline="true"
                android:contentDescription="Profile Button"
                android:focusable="true"
                android:scaleType="centerCrop"
                android:src="@drawable/baseline_person_24"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:shapeAppearanceOverlay="@style/CustomShape"
                app:strokeColor="@color/white"
                app:strokeWidth="8dp" />

            <!-- Campo para Editar Nombre -->
            <EditText
                android:id="@+id/edNombre"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="40dp"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="16dp"
                android:hint="Introduce nuevo nombre de usuario"
                android:inputType="text"
                android:maxLength="25"
                android:padding="12dp"
                android:textColor="@android:color/white"
                android:textColorHint="@android:color/white"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/BotonPerfil" />

            <!-- Contador de caracteres para 'Nombre' -->
            <TextView
                android:id="@+id/nombreCounter"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="5dp"
                android:text="0/25"
                android:textColor="@color/white2"
                android:textSize="12sp"
                app:layout_constraintTop_toBottomOf="@id/edNombre"
                app:layout_constraintEnd_toEndOf="parent" />

            <!-- Campo para Editar 'Sobre ti' -->
            <EditText
                android:id="@+id/edSobreTi"
                android:layout_width="0dp"
                android:layout_height="140dp"
                android:layout_marginTop="40dp"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="16dp"
                android:background="@drawable/border_editext"
                android:gravity="top|start"
                android:hint="Escribe algo sobre ti"
                android:inputType="textMultiLine"
                android:maxLength="300"
                android:maxLines="5"
                android:padding="12dp"
                android:textColor="@android:color/white"
                android:textColorHint="@color/white2"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/nombreCounter" />

            <!-- Contador de caracteres para 'Sobre ti' -->
            <TextView
                android:id="@+id/sobreTiCounter"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="5dp"
                android:text="0/300"
                android:textColor="@color/white2"
                android:textSize="12sp"
                app:layout_constraintTop_toBottomOf="@id/edSobreTi"
                app:layout_constraintEnd_toEndOf="parent" />

            <!-- Botón Guardar -->
            <FrameLayout
                android:layout_width="270dp"
                android:layout_height="70dp"
                android:layout_marginTop="80dp"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/sobreTiCounter">

                <ImageButton
                    android:id="@+id/botonSave"
                    android:layout_width="200dp"
                    android:layout_height="50dp"
                    android:layout_gravity="center"
                    android:background="@drawable/rounded_button_background"
                    android:contentDescription="Botón para guardar perfil" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:fontFamily="@font/pixeli"
                    android:text="Guardar"
                    android:textColor="@android:color/white"
                    android:textSize="20sp"
                    android:textStyle="bold" />
            </FrameLayout>

        </androidx.constraintlayout.widget.ConstraintLayout>
    </ScrollView>
</androidx.constraintlayout.widget.ConstraintLayout>


